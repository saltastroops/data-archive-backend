# TODO UPDATE
type User {
  id: ID! @unique
  familyName: String!
  givenName: String!
  username: String! @unique
  email: String! @unique
  affiliation: String!
  password: String!
  roles: [Role]!
}

# TODO UPDATE
enum Role {
  ADMIN
}
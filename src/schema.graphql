# import * from './generated/prisma.graphql'

"""
The schema's root query type.
"""
type Query {
  """
  The currently logged in user. An error is raised if the user is not logged in.
  """
  user: User
}

"""
The schema's root mutation type.
"""
type Mutation {
  # TODO UPDATE
  """
  Adds the newly created user when the valid information is provided and a username or an email address does not exist.
  """
  signup(
    familyName: String!
    givenName: String!
    username: String!
    email: String!
    affiliation: String!
    password: String!
  ): User!
}

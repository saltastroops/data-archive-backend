# import * from './generated/prisma.graphql'

"""
The schema's root query type.
"""
type Query {
  """
  Retrieves the specific user.
  """
  user: User
}

"""
The schema's root mutation type.
"""
type Mutation {
  #TODO
  """
  Adds the newly created user.
  """
  addUser(name: String!): User!
}

# Specify a base image.
FROM node:12-alpine AS alpine

# Project working directory.
WORKDIR /data-archive-backend

# install pm2
RUN yarn global add pm2

# Run 'yarn restart' when the container starts.
CMD ["yarn", "restart"]